robots:
  d_bot:
    default_gripper: d_bot_gripper

grippers:
  d_bot_gripper:
    type: RobotiqGripper
    args:
      prefix: d_bot_gripper_
      effort: 5.0
      velocity: 0.1
      timeout: 6.0

cameras:
  d_bot_camera: DepthCamera
